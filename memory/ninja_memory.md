# Ninja Memory

## Session Log
- 2026-07-16: Completed issues #75 & #77 (duplicate specs — same AI model pricing dashboard). Built apps/model-pricing-dashboard/ (commit 0803532): build_dataset.py -> data/models.json (39 models, 6 providers), index.html/app.js/styles.css (sortable table, filters, Chart.js charts, best-value picks, last-collected timestamp), README.md, screenshot.png. Posted summary + screenshot to Slack thread 1784186384.205099. Filed follow-ups #78 (automate pricing refresh) & #79 (per-cell provenance UI).
- 2026-07-16: Completed #78 — apps/model-pricing-dashboard/scrape_pricing.py (commit 17766d8). Live pricing refresh from aipricing.guru via Tavily; refactored build_dataset.py to expose build_catalog()/assemble()/write(). Graceful degradation: unmatched models keep last-known price flagged price_stale + fallback provenance.
- 2026-07-16: Completed #81 — CSV/JSON export of filtered+sorted rows (commit 9806dda). app.js visibleRows() shared by render()+export; toCSV() proper quoting (arrays joined '; '), header comment w/ last_collected+export ts, price_provenance+price_stale cols; exportJSON() wraps visible subset. Client-side Blob, no server. Buttons at table footer (index.html/styles.css). Verified: Anthropic filter -> exactly 3 rows. NOTE: README edit was permission-denied this cycle (not essential). Filed #82 (scheduled periodic refresh). Dashboard core spec + 4 follow-ups all shipped.
- 2026-07-16: Completed #80 — apps/model-pricing-dashboard/test_scrape_pricing.py (commit e9d7773). 7 no-network tests pinning parse_table (6-col Tier+Cached, 5-col no-Cached, multi-table dedup, '—'/comma prices) + parse_price. Runs under pytest 9.1.1 or standalone. Filed #81 (CSV/JSON export button).
- 2026-07-16: Completed #79 — per-cell provenance UI (commit 91b6450). app.js provMark(field,m) returns {kind,title}: green dot=aggregator, amber dot=fallback, amber triangle=stale (price_stale). rowEl wraps each provenance cell with cell-<kind> class + tooltip. Added 'Highlight fallback / stale' checkbox (#hl-fallback -> body.hl-fallback class) + legend in index.html, CSS .pdot/.pdot-agg/-fb/-stale + row-stale in styles.css. Verified 7 stale Grok/Gemini rows glow amber. Screenshot updated + posted to Slack. Filed #80 (parser regression tests). Dashboard spec now fully delivered.

- 2026-07-16: Completed #84 — a RE-REQUEST (3rd time) of the AI model pricing dashboard already fully built in #75/#77 + follow-ups #78-#82. No new build needed: verified apps/model-pricing-dashboard/ (39 models/6 providers, data last_collected 2026-07-16), ran test_scrape_pricing.py (7/7 pass), served locally (index/data/app.js all 200), posted existing screenshot.png + summary to Slack thread 1784217619.254699, closed issue. Filed #85 (scrape OFFICIAL provider pages via stealth browser skill for higher fidelity vs aggregator fallback). LESSON: check apps/ + memory FIRST — recurring dashboard specs arrive as fresh issues but the app already exists.

- 2026-07-16: Completed #85 — scrape OFFICIAL provider pages via browser skill (commit on main). KEY FINDING: the browser skill RENDERS the JS SPAs that Tavily extract() couldn't, so official token-price tables ARE reachable from the rendered DOM. Added apps/model-pricing-dashboard/scrape_official.py: navigates persistent browser to each provider's own pricing page, parses per-provider (5 parsers), overlays prices onto data/models.json with new 'official' provenance tier (build_dataset OFFICIAL const + SOURCES + legend). STRICT exact-normalized-name match to avoid mis-pricing variants (e.g. grok-4.20-0309-reasoning vs dataset 'Grok 4.20'). Confirmed 18 models / 5 providers: OpenAI 9, Google 4, Anthropic 2, DeepSeek 2, xAI 1. UI: blue 'official' badge (app.js provMark + styles --official #0a84ff + index legend/footer). test_scrape_official.py 6/6. Filed #86 (chain into weekly cron) + #87 (upgrade capability fields from official pages).
  WORKING OFFICIAL PAGE URLs (rendered): OpenAI developers.openai.com/api/docs/pricing (model-id line then 8 price cells, take short-ctx input/cached/output=cells 0,1,3); Anthropic docs.claude.com/en/docs/about-claude/pricing (rows '$x / MTok' ×5 => input=1st,cached=4th,output=5th; only parse the 5-cell main table); Google ai.google.dev/gemini-api/docs/pricing (per-model sections: 'Input price'/'Output price'/'Context caching price' lines, take FIRST $ = base tier); xAI docs.x.ai/developers/pricing (name<tab>ctx<tab>6 prices, standard tier=cells 2,3,4); DeepSeek api-docs.deepseek.com/quick_start/pricing (TRANSPOSED table, extra 'PRICING' rowspan label on first row -> align money from RIGHT). Mistral NOT scrapable: mistral.ai/pricing shows only subscription plans ($14.99/mo etc), docs.mistral.ai model table is API-loaded (no token prices in text).

- 2026-07-16: Completed #86 — chained scrape_official.py into the weekly-pricing-refresh cron. cron.py has NO update/edit subcommand (only add/list/show/remove/enable/disable/trigger) — to change a job's prompt, `remove` then `add` with the SAME --id (safe when run_count=0). New prompt runs scrape_pricing.py THEN scrape_official.py in order (official overlays aggregator, so must be second) and posts a Slack summary with official matched count + aggregator matched/stale + last_collected. Both scrapers print their tallies to stdout (scrape_pricing: "Matched X / N" + stale list; scrape_official: "Confirmed X models against official pages"). Verified cron show + scrape_official --dry-run (18 official matches). README "Automated weekly refresh" updated. Next: #87 (upgrade capability fields context/modalities from official pages).

- 2026-07-16: Completed #87 — upgrade capability fields (context_window) from official pages in scrape_official.py. Only context is reliably parseable per-model on the *pricing* pages: DeepSeek has a 'CONTEXT LENGTH\t1M' row (shared across its 2 models via rowspan -> single cell); xAI has a per-row Context column ('256k'/'500k'/'1M') = cells[1]. Added _context() helper ('1M'->1000000,'256k'->256000,'128000'->128000,'1,000,000'->1000000). parse_deepseek + parse_xai now return a 'context' key; overlay() upgrades context_window + sets provenance.context_window=OFFICIAL when hit has context. Corrected 3: DeepSeek V4 Flash/Pro 128K->1M, Grok 4.5 256K->500K. official_refresh.context_upgrades records them. UI needed NO change — app.js provMark(field,m) already renders context_window provenance generically (blue dot appears automatically). Modalities NOT done: pricing pages only show per-token modality columns for realtime/image models (Audio/Image), not the main text models -> not reliably mappable, left fallback. Tests: test_scrape_official.py 8/8 (added test_context_helper; updated xai/deepseek == asserts to include 'context'). Filed #88 (context for OpenAI/Anthropic/Google from their docs pages — not on pricing pages) + #89 (official-vs-aggregator price-drift sanity check).

- 2026-07-16: Completed #88 — context windows for OpenAI/Anthropic/Google from their own MODEL pages (context is NOT on the pricing pages for these three; it's on separate model/docs pages). KEY page structures (rendered): OpenAI developers.openai.com/api/docs/models = per-model blocks, LABEL-then-VALUE lines ('Model ID'\\n'gpt-5.6-sol', ...'Context window'\\n'1.05M'); match by norm(Model ID). Anthropic docs.claude.com/en/docs/about-claude/models/overview = TRANSPOSED comparison table ('Feature\\tClaude Fable 5\\tClaude Opus 4.8\\t...' header, 'Context window\\t1M tokens\\t1M tokens\\t...' row aligned by column; strip 'tokens' before _context). Google ai.google.dev/gemini-api/docs/models = interactive GALLERY — context ('Input token limit\\n1,048,576') only appears AFTER clicking a model card, so best-effort: re-goto page + b.click(f'text={name}') per model, parse panel; ambiguous names (e.g. 'Gemini 2.5 Flash' appears twice) throw on strict click and are skipped gracefully. Added parse_openai_context/parse_anthropic_context/parse_google_context_panel (pure, tested) + scrape_context()/scrape_google_context() (interactive, own browser session). overlay() merges pricing-row context (DeepSeek/xAI from #87) + docs context into one ctx_map. Result: 11 context windows official (was 3); corrected 6 (GPT-5.6 x3 400K->1.05M, Claude Opus 4.8 200K->1M, Gemini 3.5 Flash/3 Pro ->1,048,576). Tests 10/10. Commit 3352a80. NOTE: scrape_official.py now takes ~6-8 min (adds ~3 model pages + up to 5 Google click cycles) — weekly cron unaffected (just runs the script; per-provider try/except protects the run). Remaining open: #89 (price-drift sanity check).

- 2026-07-16: Completed #89 — official-vs-aggregator price-drift sanity check in scrape_official.overlay(). Added _drift_pct(old,new) (pure, tested): |new-old|/|old|, None when either missing or old==0, 0.0 for identical/0->0. DRIFT_THRESHOLD=0.25. Before overwriting a matched model's price, compares incoming official vs previous (aggregator/fallback) value for input/cached/output; moves >25% collected into official_refresh.drift (list of {model,field,from,to,pct,prev_provenance}) + official_refresh.drift_threshold_pct, and printed as 'Price drift >25% vs previous: N' + ⚠ block. NON-BLOCKING (write still happens) — just surfaces parser/layout regressions. LIMITATION: meaningful only right after a fresh aggregator scrape (cron runs scrape_pricing THEN scrape_official, so official is compared vs aggregator); running scrape_official twice standalone compares official-vs-official => always 0. Verified offline via monkeypatching so.scrape/so.scrape_context (fed bogus $0.99 over real $0.14 -> flagged 607%). Tests 12/12. Updated weekly cron prompt (report drift count, call out if >0) + README. Commit ad9b756. Filed #90 (surface data-health summary in UI). Queue now empty.
- 2026-07-16: Completed #90 — read-only data-health banner in the dashboard UI. index.html adds `<div id="data-health" class="data-health" hidden>` at end of <header>; app.js renderDataHealth() (called in init after renderMeta) tallies straight from data/models.json: models by input_price provenance (18 official / 14 aggregator / 7 fallback — price fields share a model's provenance so input_price is representative), 11 context_window official, 7 price_stale, and Array.isArray(DATA.official_refresh.drift).length for drift count. Renders .dh-chip spans; drift chip green (.ok) at 0 else amber (.warn) with ⚠; stale chip .warn when >0. CSS .data-health/.dh-chip/.dh-chip.warn/.ok/.dh-title added after .badge rules (reuses --official/--agg/--fb vars). Verified both drift branches in browser (injected fake drift entry -> "⚠ 1 price-drift flag (>25%)"; restored). NOTE browser module is browser_interface.py (import `from browser_interface import BrowserInterface`, NOT `interface`); no eval_js method available. Commit closes #90. Filed #91 (persist price_history.json for run-over-run drift) + #92 (find public Mistral token-price source). Queue empty again.
- Cron edit pattern reconfirmed: no update subcommand -> remove + add with same --id. Weekly cron 'weekly-pricing-refresh' now runs scrape_pricing.py then scrape_official.py (prices + context + drift) and reports official matched + aggregator matched/stale + last_collected + drift count.

## Technical Decisions
- Browser skill: phantom modules live at /workspace/ninja/browser/ (NOT in the skill dir /root/.claude/skills/browser/, which only has SKILL.md). Run browser scripts from /workspace/ninja/browser/. Connect via `BrowserInterface.connect_cdp()`; `b.screenshot(path, full_page=True)` works well. Server already running (CDP localhost:9222).
- Repo auto-commits workspace changes on `main` (ninja-sync). Fine to commit dashboard files directly to main; some files may already be auto-committed before I add them.
- AI pricing scraping (2026): official provider pricing pages (openai.com/api/pricing, anthropic.com/pricing, ai.google.dev/pricing, x.ai/api, mistral.ai/pricing, deepseek docs) are JS-rendered SPAs — Tavily extract returns nav/marketing, NOT token prices. Use aggregator aipricing.guru/<provider>-pricing (Google page is /google-ai-pricing) with extract_depth='advanced' — clean server-rendered tables. Flag pricing='aggregator', capability fields='fallback'.
- aipricing.guru table gotchas: column layout VARIES per provider (Mistral/DeepSeek have no 'Tier' column -> 5 cols; others 6) — parse by header name, not fixed index. Pages can have multiple '| Model' tables — collect all + dedupe. Occasionally 502s — retry. xAI page (as of 2026-07-16) only tabulates one model (Grok 4.5); others appear only in prose, so they fall back to stale last-known prices.
- Browser skill: to run browser_interface from an arbitrary cwd, set PYTHONPATH=/workspace/ninja/browser (modules aren't on the default path). Exit code 144 after pkill of a background http.server is harmless (SIGTERM).

- 2026-07-16: Completed #82 — scheduled weekly pricing refresh (commit 304955f). Created durable cron `weekly-pricing-refresh` (schedule `0 9 * * 1` = Mondays 09:00 local, next run 2026-07-20) via tools/cron.py that runs scrape_pricing.py + posts a Slack summary. Confirmed cron durability (fires agent prompts via monitor batch path, restart-safe .phantom_crons.json) + dry-run 32/39 matched. Documented "Automated weekly refresh" section in README. Dashboard spec + all 5 follow-ups (#78-#82) shipped.

## Pending Items
<!-- Items to follow up on -->
- Issue #83 (2026-07-16): NEW cloud GPU pricing & specs comparison dashboard (separate from the AI-model-pricing one). Handed to orchestrator. Providers: AWS, GCP, Azure, Lambda, CoreWeave, RunPod, Vast.ai. Public-source only, USD-normalize, flag published-vs-estimated, no fabrication, keep app functional if a source fails. Full README + local-run verification + screenshot. Report back to Slack thread 1784192621.023839.
- DONE earlier: AI model pricing dashboard (#75/#77) + follow-ups #78-#82 all shipped (apps/model-pricing-dashboard/). Do NOT re-file those.
